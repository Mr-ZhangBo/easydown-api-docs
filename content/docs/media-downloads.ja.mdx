---
title: "APIレスポンスからメディアをダウンロード"
description: "APIが返す動画・画像・音声URL、必要な公開ヘッダー、品質情報、リダイレクト、有効期限を正しく扱います。"
---

`media`オブジェクトには`images`、`videos`、`audios`の配列があります。

## レスポンス項目

- `url`：公開または有効期限付きのメディアURL。
- `quality`、`mimeType`、`width`、`height`、`bitrate`：メディア形式を選ぶための情報。
- `source`：`direct`、`adaptive`、`dash`、`live_photo`、`native_voice`のいずれか。
- `headers`：メディア配信元が必要とする`Referer`などの公開リクエストヘッダー。

## 制限と動作

ダウンロードURLには有効期限があります。解析後すぐに取得し、リダイレクトに従い、クエリ文字列を維持し、返された公開ヘッダーだけを送信してください。完全なプラットフォームデータはSupabaseに保存されません。
